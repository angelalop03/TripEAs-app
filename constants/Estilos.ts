// Tokens visuales compartidos (sombras) para que las tarjetas/botones de
// toda la app usen exactamente los mismos valores en vez de copias sueltas
// con ligeras variaciones entre pantallas.
export const SOMBRA_TARJETA = {
  shadowColor: '#000000',
  shadowOpacity: 0.06,
  shadowOffset: { width: 0, height: 2 },
  shadowRadius: 8,
  elevation: 2,
};

export const SOMBRA_TARJETA_SUAVE = {
  shadowColor: '#000000',
  shadowOpacity: 0.04,
  shadowOffset: { width: 0, height: 1 },
  shadowRadius: 4,
  elevation: 1,
};

export const SOMBRA_BOTON_TURQUESA = {
  shadowColor: '#00E9B0',
  shadowOpacity: 0.3,
  shadowOffset: { width: 0, height: 4 },
  shadowRadius: 12,
  elevation: 4,
};
